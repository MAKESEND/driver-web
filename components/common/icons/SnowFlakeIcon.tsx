import { styled } from '@mui/material';
import AcUnit from '@mui/icons-material/AcUnit';

export const SnowFlakeIcon = styled(AcUnit)(({ theme }) => ({
  color: theme.palette.common.blue50,
}));

export default SnowFlakeIcon;
